// Durations for the rAF-driven animations, in ms. globals.css mirrors these
// as --d-draw and --d-roll for CSS use; change both together.
export const DURATION_DRAW = 700;
export const DURATION_ROLL = 900;
